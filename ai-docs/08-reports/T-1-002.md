# T-1-002. Правка docker-compose: postgres в compose, env_files, сети

> **Автор:** Claude Sonnet (backend-кодер)
> **Дата:** 2026-04-24
> **Статус:** done

## Что сделано
- ✅ Добавлен сервис `db` (postgres:16-alpine) с healthcheck и named volume `pgdata`
- ✅ Redis переведён на `redis:7-alpine` + healthcheck + named volume `redisdata`
- ✅ `web` зависит от db/redis через `condition: service_healthy` — больше нет race condition
- ✅ `env_file: Config/.env` → `env_file: .env` (T-1-007: .env в корне)
- ✅ Все worker-сервисы (tg_sender, manage_control, daily_checker) получили `env_file` и `depends_on`
- ✅ `DATABASE_PASSWORD:?` — docker-compose падает с ошибкой если переменная не задана

## Отклонения
- `image: ms-service:1.0.1` → `ms-service:latest` — версионирование образов через CI, не вручную
