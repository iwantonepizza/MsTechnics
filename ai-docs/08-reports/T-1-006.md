# T-1-006. Критические баги

> **Автор:** Claude Sonnet (backend-кодер)
> **Дата:** 2026-04-24
> **Статус:** done

## Что сделано

### SEC-007: registration view
- ✅ `user/views.py`: функция `registration()` удалена полностью
- ✅ `user/urls.py`: маршрут `/user/registration/` удалён
- Комментарий в urls.py объясняет причину удаления

### MDL-009: signal sender класс вместо строки
- ✅ `application/signals.py`: `user=sender` (класс) → `user="system"` (строка)
- NOTE оставлен: в Фазе 2 заменить на FK(User) через ActivityLog

### MDL-010: if Panels (класс, всегда truthy)
- Поиск по коду показал что в текущей кодовой базе паттерн `if Panels:` отсутствует.
  `if panel:` везде корректен (проверяет instance, не класс). Баг закрыт как false-positive.

## Отклонения
- Баг MDL-010 не воспроизведён в текущем коде — возможно уже был исправлен ранее
