# T-1-007. django-environ, .env.example, секреты

> **Автор:** Claude Sonnet (backend-кодер)
> **Дата:** 2026-04-24
> **Статус:** done (code part) / ⚠️ requires owner action

## Что сделано
- ✅ `MsServiceControl/settings.py` переписан с `django-environ`
- ✅ `SECRET_KEY` — fail-fast при отсутствии: `RuntimeError` при старте Django
- ✅ `DEBUG` — правильное bool-приведение через `env("DEBUG")` (строка "False" → False)
- ✅ `ALLOWED_HOSTS` — читается из env, не захардкожен
- ✅ `.env.example` создан в корне проекта
- ✅ `.gitignore` обновлён: `Config/client_secret.json`, `token.pickle`, `credentials.json`
- ✅ Обратная совместимость: читает `Config/.env` если `.env` в корне нет

## ⚠️ Требует действий владельца (НЕ кодера)
1. Отозвать OAuth secret `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` в Google Cloud Console
2. Создать новый OAuth client, сохранить JSON локально (НЕ в репо)
3. Запустить `git filter-repo --invert-paths --path Config/client_secret.json` для чистки истории
4. Сменить пароль пользователя `izhevsk11` в Django admin прода
