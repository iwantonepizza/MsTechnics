# T-1-011. Скрыть архивные заявки из дашбордов

> **Автор:** Claude Sonnet (backend-кодер)
> **Дата:** 2026-04-24
> **Статус:** done

## Что сделано
- ✅ `application/models.py`: добавлен `ApplicationManager` с методом `all_new()`
  и константой `ARCHIVE_STATUSES = ("archive_done", "archive_unable")`
- ✅ `Application.objects = ApplicationManager()` — менеджер по умолчанию
- ✅ `main_menu/views.py`: `.all()` → `.all_new()`
- ✅ `control/views.py`: ручной `.exclude(status__in=[...])` → `.all_new()` — единая точка правды
- ✅ Архивные вкладки (`application_report`, history) — оставлены с `.all()` намеренно
