# T-2-027. DisplayService.create_with_layout()

> **Дата:** 2026-04-24 | **Статус:** done

## Что сделано
- ✅ `apps/directory/displays/services.py` — DisplayService + DisplayLayoutSpec
- ✅ `Display.save()` очищен от side-effects — теперь стандартный Django
- ✅ `DisplayWithLayoutFactory` добавлена в tests/factories.py
- ✅ 5 тестов: layout создаётся правильно, ValueError при rows=0, save() без side-effects

## Compat
- Старый код `Display.objects.create(name=..., rows=N, cols=M)` больше НЕ создаёт ячейки
- Для создания с layout: `display_service.create_with_layout(DisplayLayoutSpec(...))`
