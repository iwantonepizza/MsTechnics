# T-3-fix-001. Status names sync

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что проверено

- Миграция `apps/workflow/applications/migrations/0004_strip_application_prefix.py` уже есть.
- Миграция переименовывает legacy application statuses:
  - `application_sent_to_control` -> `sent_to_control`;
  - `application_apply_in_control` -> `apply_in_control`;
  - `application_sent_to_service` -> `sent_to_service`;
  - `application_work_in_service` -> `work_in_service`;
  - `application_unable` -> `unable`.
- Reverse-функция возвращает старые имена.
- `apps/workflow/applications/state_machine.py` использует статусы без `application_`.
- `frontend/src/pages/display-view/DisplayViewPage.tsx` использует `ROLE_TRANSITIONS` без `application_`.

---

## Проверки

- `rg "application_sent_to_control|application_apply_in_control|application_sent_to_service|application_work_in_service|application_unable" apps shared frontend/src application departure main user zip -g "*.py" -g "*.ts" -g "*.tsx" -g "!**/migrations/**"` — пусто.
- `apps/workflow/applications/migrations/0004_strip_application_prefix.py` прочитан, dependency указан на `0003_panel_fk_to_id`.
- `ai-docs/08-reports/architect-review-after-hotfixes.md` уже подтверждает hotfix как закрытый архитектором.

Что не прогнано:
- `showmigrations`/`migrate`: локальный PostgreSQL host из env недоступен (`getaddrinfo failed`).
- `pytest`: в `.venv` нет `pytest`.

---

## Вывод

Кодовая часть T-3-fix-001 уже реализована и синхронизирована с контрактом. Задача переведена из `ready` в `review`, чтобы её не брали повторно.
