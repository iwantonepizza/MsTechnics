# T-4-001. Design tokens

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- `frontend/src/app/styles/tokens.css` подключен первым в `frontend/src/main.tsx`.
- Tailwind настроен на CSS variables из design tokens.
- `frontend/src/app/styles/globals.css` подключает базовые стили и Tailwind directives.
- В `App.tsx` добавлена автоматическая установка `data-density`: `touch` для coarse pointer, `comfortable` для desktop.

---

## Тесты

- `npm run typecheck` — зелено.
- `npm run test` — зелено.
- `npm run build` — зелено, есть только Vite warning о размере чанка.
- `npm run test:e2e` — зелено.

---

## Отклонения

- В коде еще встречаются inline `style={{ ... }}` с CSS variables. Это не нарушает token model, но полная замена на Tailwind utility-классы может быть отдельной polish-задачей.
