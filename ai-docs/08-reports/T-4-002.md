# T-4-002. OpenAPI types

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- `api-schema.yaml` генерируется через `manage.py spectacular`.
- `frontend/src/shared/api/schema.d.ts` генерируется через `npm run generate:api-types`.
- `frontend/src/shared/api/types.ts` использует generated schemas как базу для frontend-типов.
- После добавления `/executors/` схема и generated types обновлены.
- Follow-up детали и исправления схемы описаны в `ai-docs/08-reports/T-4-002-followup.md`.

---

## Тесты

- `.venv\\Scripts\\python manage.py spectacular --skip-checks --validate --file api-schema.yaml` — зелено с существующими drf-spectacular warnings/errors.
- `npm run generate:api-types` — зелено.
- `npm run typecheck` — зелено.
- `npm run test` — зелено.
- `npm run build` — зелено.
- `npm run test:e2e` — зелено.

---

## Отклонения

- Команда схемы использует `--skip-checks`, потому что обычный Django system check падает на существующие legacy-дубли моделей и таблиц.
