# T-4-004. AppLayout + Header

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- `frontend/src/widgets/navigation/AppLayout.tsx` оборачивает авторизованные страницы, держит header и outlet.
- `frontend/src/widgets/navigation/Header.tsx` показывает основную навигацию, пользователя, logout, counts и SSE indicator.
- `CrumbContext` подключен в layout.
- Header фильтрует пункты по роли пользователя.
- Follow-up 2026-05-05: `useNavCounts` переведён на общий `/dashboard/` query с `queryKey: ['dashboard']`, чтобы не делать дублирующие `/applications/?box=received` запросы для monitoring/control.

---

## Тесты

- `npm run typecheck` — зелено.
- `npm run test` — зелено.
- `npm run build` — зелено.
- `npm run test:e2e` — зелено ранее; в follow-up не перезапускался.

---

## Отклонения

- User dropdown пока заменен на компактный user chip + logout button.
- Глобальный поиск в header не включался, потому что backend/search contract не зафиксирован.
