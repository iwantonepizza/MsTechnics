# T-4-010. LoginPage

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- `frontend/src/pages/login/LoginPage.tsx` содержит login/password форму.
- Валидация пустых полей сделана через `react-hook-form` + `zod`.
- При успешном входе access token сохраняется в auth store и пользователь уходит на `/menu`.
- При ошибке входа показывается toast.
- Уже авторизованный пользователь редиректится с `/login` на `/menu`.

---

## Тесты

- `npm run typecheck` — зелено.
- `npm run test` — зелено.
- `npm run build` — зелено.
- `npm run test:e2e` — зелено: login page renders.

---

## Отклонения

- Field-level обработка backend 422 не выделялась отдельно: сейчас backend detail показывается toast'ом.
