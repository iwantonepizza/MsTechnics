# T-4-012. DepartmentListPage

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- ✅ `DepartmentListPage` переработан в layout `1fr + 320px` с городами/экранами слева и правым рейлом.
- ✅ Поддержаны пути `/monitoring`, `/control`, `/service` и city-level варианты `/:department/:citySlug`.
- ✅ Экранные карточки показывают название, размеры, техническое имя и переходят в DisplayView того же отдела.
- ✅ Правый рейл уникален по отделу: monitoring/control/service используют разные заголовки и box-фильтры заявок.
- ✅ Добавлены loading, empty и error states с retry.
- ✅ `useApplications` больше не отключает запрос без `display`, поэтому очереди отделов загружаются на department-level страницах.

---

## Отклонения от плана

- **Что:** проблемы по экрану считаются нейтрально, без `current_condition`.
- **Почему:** текущий `DisplayList` schema не отдаёт `current_condition`; добавлять backend-поле вне карточки не стал.
- **Нужно ли обновить архитектурный документ:** нет.

---

## Архитектурные решения

- City-level route реализован как фильтр существующего компонента, а не новая страница.
- Right rail переиспользует `ApplicationCard compact`, чтобы не плодить новый формат заявки.

---

## Тесты

- Новых файлов с тестами: 0
- Добавлено тестов: 0
- Coverage нового кода: N/A
- Проверка: `npm run typecheck` — зелёное.

---

## Нагрузка / производительность

- Страница грузит `/cities/`, `/displays/`, `/applications/`.
- City-filter применяется на клиенте к уже загруженному списку экранов.

---

## Миграции

N/A

---

## Время

- Оценка в задаче: 3 часа
- Фактически: около 1 часа

---

## Проверки перед PR

- [x] `npm run typecheck` — зелёное
- [x] Нет debug-кода
- [x] Нет секретов

---

## Скриншоты / демо

N/A

---

## Дальнейшие шаги

- Если нужен реальный индикатор проблем на карточке экрана, добавить `current_condition` или aggregate counts в `DisplayListSerializer`.

---

## Вопросы архитектору / ретро

- Нужно ли фильтровать правый рейл по active city на backend через `city` query param.
