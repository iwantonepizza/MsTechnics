# T-4-016. ZipPage — Storage + расходники + history

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- ✅ ZIP page оставлена в 6-колоночном layout: 4 колонки панелей, расходники, история.
- ✅ Исправлена фильтрация панелей по экрану: panels API ждёт `display_id`, поэтому slug преобразуется в `Display.id`.
- ✅ Selector экрана теперь синхронизирует URL `/zip` и `/zip/:displaySlug`.
- ✅ Click на панель выбирает её и показывает детали в history rail.
- ✅ Колонки панелей показывают counts и selected state.
- ✅ Расходники продолжают фильтроваться по slug, как ожидает storage API.
- ✅ История фильтруется по display и `kind=panel.`.

---

## Отклонения от плана

- **Что:** кнопка `+ Панель` добавлена disabled.
- **Почему:** backend endpoint создания панели не описан в карточке и не должен додумываться кодером.
- **Нужно ли обновить архитектурный документ:** нет.

---

## Архитектурные решения

- URL остаётся source of truth для выбранного экрана.
- Детали выбранной панели встроены в правый rail вместо отдельной модалки, потому что карточка требует click → detail/modal, а detail без нового mutation API безопаснее.

---

## Тесты

- Проверка: `npm run typecheck` — зелёное.

---

## Нагрузка / производительность

- ZIP page делает 4 запроса панелей, 3 запроса storage, 1 запрос activity.
- Virtualization не добавлялась: в текущем scope нет зависимости `@tanstack/react-virtual`, а список рендерится компактно. Если в колонках будет стабильно 100+ элементов, нужна отдельная задача.

---

## Миграции

N/A

---

## Время

- Оценка в задаче: 3 часа
- Фактически: около 0.5 часа

---

## Проверки перед PR

- [x] `npm run typecheck` — зелёное
- [x] Нет debug-кода
- [x] Нет секретов

---

## Дальнейшие шаги

- Добавить backend endpoint создания панели, если кнопка `+ Панель` нужна в SPA.
- Добавить virtualization при больших объёмах панелей.
