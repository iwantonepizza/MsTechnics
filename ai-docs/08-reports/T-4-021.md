# T-4-021 / T-4-022 / T-4-023. Specialized panel modals

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- Добавлен общий набор специализированных модалок в `frontend/src/features/panels/PanelActionModals.tsx`.
- Реализованы `ChangeConditionModal`, `ChangeDepartmentModal`, `MoveToCellModal`.
- `DisplayViewPage` получил действия для выбранной панели: смена состояния, смена отдела, постановка свободной панели в пустую ячейку.
- В `frontend/src/entities/panel/hooks.ts` добавлены mutation hooks для `PATCH /panels/:id/` и `POST /panels/:id/move-to-cell/`.
- `ChangeDepartmentModal` блокирует submit при активной заявке и показывает предупреждение до запроса.
- `MoveToCellModal` выбирает свободные панели из `zip` и `hand`.

---

## Отклонения от плана

- `RemoveFromCellModal` не реализован.
- Причина: backend endpoint `POST /api/v1/panels/<id>/remove-from-cell/` отсутствует в фактическом API-контракте, поэтому UI не должен отправлять выдуманный запрос.
- `CreateApplicationModal` уже был в проекте и продолжает использоваться из `DisplayViewPage`; в этой задаче он не переписывался полностью.

---

## Тесты

- `npm run typecheck` — зелено.
- `npm run test` — зелено.
- `npm run build` — зелено, есть только предупреждение Vite о размере чанка.
- `npm run test:e2e` — зелено после установки Chromium для Playwright.

---

## Дальше

- Для полного закрытия RemoveFromCell нужен backend action и OpenAPI-схема.
- Для optimistic rollback по панельным операциям лучше сделать отдельную точечную задачу после стабилизации API ответов.
