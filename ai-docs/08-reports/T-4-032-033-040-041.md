# T-4-032 / T-4-033 / T-4-040 / T-4-041. States, shortcuts, tests

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- Добавлен `frontend/src/shared/lib/useKeyboard.ts` с игнором `input`, `textarea`, `contenteditable` и поддержкой `Mod+Enter`.
- Добавлен `frontend/src/shared/ui/ShortcutsHelp.tsx`.
- `App.tsx` открывает глобальный help overlay по `?`.
- `DisplayViewPage` получил контекстные shortcuts для service/control/monitoring действий.
- Настроен Vitest: `frontend/vitest.config.ts`, `frontend/src/test/setup.ts`.
- Добавлены unit-тесты:
  - `frontend/src/shared/lib/useDeferredLoading.test.tsx`
  - `frontend/src/shared/lib/useKeyboard.test.tsx`
- Настроен Playwright: `frontend/playwright.config.ts`.
- Добавлены smoke e2e:
  - login page renders
  - `/menu` требует авторизацию и редиректит на `/login`

---

## Отклонения от плана

- Coverage threshold не включался, потому что в проекте еще нет достаточной базы unit-тестов по auth/api/application hooks.
- Полные e2e сценарии transition и SSE не добавлены: им нужны стабильные seed users/data и backend e2e fixture.
- Help overlay содержит видимый список shortcuts, потому что это прямо требуется карточкой T-4-033.

---

## Тесты

- `npm run typecheck` — зелено.
- `npm run test` — зелено: 2 files, 4 tests.
- `npm run build` — зелено, есть только предупреждение Vite о размере чанка.
- `npm run test:e2e` — зелено: 2 tests.

---

## Среда

- Playwright Chromium установлен через `npx playwright install chromium`.
- `frontend/package-lock.json` после npm install удален, чтобы не смешивать lockfile с ожидаемым pnpm workflow.

---

## Дальше

- Добавить seed-команду для e2e.
- Расширить unit-тесты на API refresh/interceptor и application transition rollback.
- Добавить e2e для transition после появления стабильных тестовых учеток и данных.
