# T-5-001. Notifications architecture

> **PR:** N/A
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-05
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- Добавлены модели `NotificationTemplate`, `Notification`, `NotificationDeliveryAttempt`.
- Добавлена ручная миграция `apps/notifications/migrations/0001_initial.py`.
- Добавлен `BaseChannel` и stub-каналы `TelegramChannel`, `MaxChannel`, `EmailChannel`.
- Добавлен `NotificationDispatcher` с fallback order `telegram -> max -> email`.
- Dispatcher пишет `NotificationDeliveryAttempt` на каждую реальную попытку, пропускает недоступные каналы и переводит уведомление в `sent` или `failed`.
- Добавлены unit-тесты dispatcher на success, fallback, skip, all failed, missing channel.
- Обновлен `ai-docs/06-integrations/notifications-redesign.md`.

---

## Отклонения

- Реальные внешние отправки не реализованы: это scope T-5-002/T-5-003/T-5-004.
- `AppConfig.ready()` пока не подключает triggers, потому что это scope T-5-006.

---

## Проверки

- `$env:DJANGO_SETTINGS_MODULE='Config.settings'; .venv\\Scripts\\python -c "import django; django.setup(); ..."` — импорт notifications OK.
- `.venv\\Scripts\\python -m compileall -q apps\\notifications` — зелено.
- `.venv\\Scripts\\python manage.py spectacular --skip-checks --validate --file api-schema.yaml` — зелено с существующими drf-spectacular warnings/errors.

---

## Что не удалось прогнать

- `manage.py makemigrations notifications --check --dry-run --skip-checks` падает на существующем migration graph проекта: `KeyError: 'display'`.
- `manage.py test apps.notifications --skip-checks` недоступен: текущая команда Django test не принимает `--skip-checks`.
- `manage.py check` ранее падает на существующие legacy-дубли моделей/таблиц, не связанные с T-5-001.

---

## Дальше

- T-5-002/T-5-003/T-5-004: заменить stub-каналы на реальные Telegram/MAX/Email delivery implementations.
- T-5-006: подключить triggers и seed templates.
