# T-5-fix-003 — live DB verification

> Follow-up 2026-05-20: restored prod-copy acceptance later uncovered an unfinished schema-alignment tail for `application.status_id` and style FKs in `application_status` / `condition` / `department`. See [T-5-fix-003-followup-2026-05-20.md](/abs/path/C:/Users/miser/mstechnics/mstechnics/ai-docs/08-reports/T-5-fix-003-followup-2026-05-20.md).

## Что сделано

- Поднят локальный PostgreSQL в контейнере `mst-pg-verify`, восстановлен прод-дамп `db_dumps/mstechnics.dump`.
- Прогнаны `manage.py migrate --plan` и `manage.py migrate` на:
  - чистой БД `mstechnics_clean_recheck`
  - копии прод-БД `mstechnics_verify_recheck`
- Исправлены блокеры migration graph:
  - `apps/core/users/migrations/0001_initial_state_import.py`
  - `main/migrations/0003_delete_cities_remove_condition_color_and_more.py`
  - `apps/workflow/applications/migrations/0001_initial_state_import.py`
  - `apps/workflow/departures/migrations/0001_initial_state_import.py`
- Добавлена физическая alignment-миграция `apps/core/users/migrations/0003_align_user_physical_schema.py`:
  - добавляет `user.max_id`
  - расширяет `user.telegram_id` до `varchar(20)`
- Добавлены реальные forward-only миграции конверсии legacy string-FK в bigint FK:
  - `apps/directory/displays/migrations/0005_convert_display_city_fk_to_id.py`
  - `apps/directory/panels/migrations/0004_convert_panel_fk_storage_to_id.py`
  - `apps/directory/displays/migrations/0006_convert_cell_fk_storage_to_id.py`
- Для локального запуска и тестов добавлен `whitenoise>=6.8` в `pyproject.toml`.

## Что сломано было фактически

- На прод-копии и на чистой БД `display.city_id`, `panel.display_id`, `panel.condition_id`, `panel.department_id`, `cell.display_id`, `cell.panel_id` физически оставались `varchar` и ссылались на legacy `name`.
- При этом Django state после alignment-миграций уже считал эти связи FK на `id`.
- Из-за этого `migrate` проходил, но реальный HTTP smoke падал на `/api/v1/displays/` с `ProgrammingError: operator does not exist: character varying = bigint`.

## Верификация

- `manage.py migrate --plan` на чистой БД: успешно
- `manage.py migrate` на чистой БД: успешно
- `manage.py migrate --plan` на копии прод-БД: успешно
- `manage.py migrate` на копии прод-БД: успешно
- ORM smoke на копии прод-БД:
  - `MsUser.objects.count()` → `7`
  - `Display.objects.count()` → `8`
  - `Panel.objects.count()` → `2333`
  - `Application.objects.count()` → `10`
  - `Cell.objects.select_related("display", "panel")` работает
- HTTP smoke на копии прод-БД:
  - `GET /api/v1/health/live` → `200`
  - `GET /admin/` → `302` на login
  - `POST /api/v1/auth/login/` → успешный access token
  - `GET /api/v1/displays/` с JWT → `200`, вернулось `8` записей
- `pytest -x --no-cov` → `79 passed`
- `pytest --cov=apps --cov=shared --cov-report=term-missing` → `79 passed`, total coverage `57%`

## Schema diff

- Финальный diff между clean и prod-after сохранён в `logs/t5_fix_003_schema_diff.txt`.
- После фиксов содержательные различия по `user`, `display`, `panel`, `cell` исчезли.
- Остались только legacy-различия по `photo_display`:
  - имена sequence (`photo_display_id_seq` vs `zip_photodisplay_id_seq`)
  - имена PK / index / FK (`photo_display_*` vs `zip_photodisplay_*`)
  - служебный токен `\restrict` от `pg_dump`

## Mail / main_menu

- Поиск `mail.Alarm|mail.GmailMessage` в `apps/` и `shared/` вернул пустой результат.
- После 2 недель stable в проде legacy-таблицы `mail_alarm` и `mail_gmailmessage` можно убирать в рамках `T-5-050`.
- `main_menu` в этом fix не менялся; его cleanup остаётся в `T-5-050..053`.

## Артефакты

- `logs/t5_fix_003_recheck_plan_prod.log`
- `logs/t5_fix_003_recheck_apply_prod.log`
- `logs/t5_fix_003_recheck_plan_clean.log`
- `logs/t5_fix_003_recheck_apply_clean.log`
- `logs/t5_fix_003_recheck_smoke_prod.log`
- `logs/t5_fix_003_recheck_smoke_clean.log`
- `logs/t5_fix_003_recheck_pytest.log`
- `logs/t5_fix_003_coverage.log`
- `logs/t5_fix_003_schema_clean.sql`
- `logs/t5_fix_003_schema_prod_after.sql`
- `logs/t5_fix_003_schema_diff.txt`

## Вывод

Задача закрывает реальный риск прод-выкатки: миграции теперь не только строят корректный Django state, но и доводят физическую схему legacy-таблиц до id-based FK, на которых уже работает текущий backend/API. Прод-дамп и чистая БД проходят полный цикл `restore → migrate → smoke`.
