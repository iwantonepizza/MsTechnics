# T-6-004. .gitignore + проверка утечки прод-данных в git

> **PR:** not opened
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-13
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- Обновлен `.gitignore`: добавлены дампы БД, логи, `staticfiles/`, `*.egg-info/`, coverage-артефакты и локальные секреты (`Config/.env`, `Config/client_secret.json`, `*.pickle`).
- Создан [ai-docs/04-conventions/security-conventions.md](/C:/Users/miser/mstechnics/mstechnics/ai-docs/04-conventions/security-conventions.md) с правилами, что не коммитим и как действовать при утечке.
- Задача [T-6-004-gitignore-and-dump-leakage.md](/C:/Users/miser/mstechnics/mstechnics/ai-docs/03-tasks/phase-5-integrations/T-6-004-gitignore-and-dump-leakage.md) переведена в `review`.
- Из индекса сняты tracked артефакты без удаления с диска: `db_dumps/`, `logs/`, `staticfiles/`, `Config/.env`, `Config/client_secret.json`.
- Установлен `git-filter-repo`, переписана история по чувствительным путям и выполнен `git push --force --all origin` вместе с `git push --force --tags origin`.

---

## Отклонения от плана

- **Что:** в истории и в текущем `HEAD` обнаружены не только дампы/логи, но и tracked локальные секреты `Config/.env` и `Config/client_secret.json`.
- **Почему:** текущий `.gitignore` явно разигноривал эти пути.
- **Нужно ли обновить архитектурный документ:** нет.

---

## Архитектурные решения

- История переписывалась только после явного подтверждения владельца.
- В purge включены не только дампы и логи из карточки, но и реально найденные в истории `Config/.env` и `Config/client_secret.json`.

---

## Тесты

- Новых файлов с тестами: 0
- Добавлено тестов: 0
- Coverage нового кода: N/A
- Что покрыто:
  - Проверка `git log --all --full-history` по дампам, логам, `staticfiles/` и локальным секретам
  - Проверка tracked путей через `git ls-files`
  - Проверка игнорирования через `git status --short --ignored`
  - Проверка после rewrite: чувствительные пути больше не находятся в `git log --all` и `git rev-list --objects --all`
- Что НЕ покрыто и почему:
  - Автотесты проекта не запускались: задача ограничивалась git history и документацией

---

## Нагрузка / производительность

N/A

---

## Миграции

N/A

---

## Время

- Оценка в задаче: 0.5-2 часа
- Фактически: < 1 часа на безопасную часть

---

## Проверки перед PR

- [ ] `pre-commit run --all-files` — не запускался, задача про git hygiene и docs
- [ ] `pytest` — не запускался, код выполнения не менялся
- [ ] `mypy` — не запускался, код выполнения не менялся
- [x] Ручное тестирование: git-история и tracked paths проверены локально
- [x] Нет debug-кода
- [x] Нет секретов в истории git
- [ ] PR-description заполнен по шаблону

---

## Дальнейшие шаги

- Всем старым клонам репозитория нужен fresh clone или жесткий resync, иначе в локальной истории артефакты останутся.
- Владельцу нужно оценить ротацию Google OAuth credentials и необходимость принудительного сброса пользовательских паролей.

---

## Вопросы архитектору / ретро

- В `HEAD` и истории уже есть чувствительные артефакты на GitHub remote `origin`; это не просто hygiene, а действующий security incident.
