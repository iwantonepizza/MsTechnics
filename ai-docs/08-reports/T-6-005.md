# T-6-005. Ротация утёкших секретов после T-6-004

> **PR:** не открыт
> **Автор:** GPT-5 Codex + владелец
> **Дата:** 2026-05-18
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- Задача переведена в `in-progress`.
- Добавлен `scripts/check_gmail_oauth.py` для безопасной проверки Gmail OAuth без запуска полного `pull_vnnox_alarms`.
- В `apps/integrations/gmail_alarms/services.py` добавлен reusable helper `check_gmail_oauth(...)` и path helpers для `client_secret.json` / `token.pickle`.
- `.env.example` очищен от bootstrap-значений, похожих на реальные секреты; оставлены явные placeholder-строки.
- Создан incident report `ai-docs/08-reports/security-incident-2026-05-13.md` как единая точка фиксации фактов и owner-side ротаций.

---

## Отклонения от плана

- **Что:** coder-side часть закрыта отдельно от полной приёмки задачи.
- **Почему:** большая часть T-6-005 требует owner-only доступов к Google Cloud Console, BotFather, dev.max.ru и прод-БД.
- **Нужно ли обновить архитектурный документ:** нет.

---

## Архитектурные решения

- Проверка Gmail OAuth оформлена отдельным script-level smoke check, а не через management command, чтобы не смешивать верификацию секретов с ingest бизнес-данных.
- В reusable helper возвращается только безопасная диагностическая информация: пути файлов, query, `result_size_estimate`, ids первых сообщений. Секреты и содержимое писем не логируются.

---

## Тесты

- Новых файлов с тестами: 1
- Добавлено тестов: 3
- Coverage нового кода: не измерял отдельно
- Что покрыто:
  - Разрешение относительного `GOOGLE_CLIENT_SECRET_PATH`
  - Happy path `check_gmail_oauth(...)` с mocked Gmail service
  - Ошибка при отсутствии `token.pickle`
  - Регрессия парсера `gmail_alarms` не сломана
- Что НЕ покрыто и почему:
  - Реальный вызов Gmail API требует owner secrets и сетевой доступ к боевому Google account
  - `scripts/check_gmail_oauth.py` не тестировался end-to-end отдельно; логика покрыта на уровне service helper

---

## Нагрузка / производительность

N/A

---

## Миграции

N/A

---

## Время

- Оценка в задаче: 1-2 часа
- Фактически: < 1 часа на coder-side часть

---

## Проверки перед PR

- [ ] `pre-commit run --all-files` — не запускался
- [x] `pytest` — `apps/integrations/gmail_alarms/tests/test_services.py apps/integrations/gmail_alarms/tests/test_parsers.py`
- [ ] `mypy` — не запускался
- [ ] Ручное тестирование owner-side ротаций — требует доступов владельца
- [x] Нет debug-кода
- [x] Нет новых секретов
- [x] `ruff check` по изменённым Python-файлам
- [ ] PR-description заполнен по шаблону

---

## Дальнейшие шаги

- Владелец выполняет фактическую ротацию секретов по шагам 1-8 и 10 из карточки.
- После owner-side действий задача может перейти из `review` в `done`; coder-side часть уже завершена.

---

## Вопросы архитектору / ретро

- Стоит зафиксировать в conventions, что `.env.example` хранит только явные placeholders, а не bootstrap-значения, похожие на реальные секреты.
