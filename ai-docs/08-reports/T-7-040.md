# T-7-040. Prod-copy migrate smoke for T-7-005

> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-20
> **Статус задачи в реестре:** review

---

## Что сделано

- Поднята временная PostgreSQL-копия `t7040-pg` на базе `db_dumps/mstechnics.dump`.
- На restored prod-copy прогнан `manage.py migrate --noinput`.
- Отдельно провалидирована именно storage-часть `T-7-005`:
  - до `migrate` в `wires_zip`, `hubs_zip`, `lamels_storage` не было `low_stock_threshold`;
  - таблиц `power_blocks_zip` и `connectors_zip` не было;
  - после `migrate` применена `directory_storage.0003_add_powerblocks_connectors_threshold`;
  - существующие записи в `wires_zip`, `hubs_zip`, `lamels_storage` получили `low_stock_threshold = 3`.

---

## SQL-факты

До `migrate`:

- `wires_zip`, `hubs_zip`, `lamels_storage` имели только `id/name/description/count/photo`
- `power_blocks_zip`, `connectors_zip` отсутствовали
- в `django_migrations` не было ни одной записи для `directory_storage`

После `migrate`:

- Таблицы:
  - `connectors_zip`
  - `hubs_zip`
  - `lamels_storage`
  - `power_blocks_zip`
  - `wires_zip`
- Existing row backfill:
  - `wires_zip`: `19` строк, `min_threshold = 3`, `max_threshold = 3`
  - `hubs_zip`: `5` строк, `min_threshold = 3`, `max_threshold = 3`
  - `lamels_storage`: `3` строки, `min_threshold = 3`, `max_threshold = 3`
  - `power_blocks_zip`: `0` строк
  - `connectors_zip`: `0` строк

---

## Проверки

- `python manage.py migrate --noinput` на temp DB `mstechnics_t7040`
- `python manage.py check`
- `pytest apps/interface/tests/test_storage.py`

---

## Результат

- Миграция `T-7-005` безопасно применяется на restored prod-copy.
- Schema-diff по storage соответствует ожидаемому объёму: 2 новые таблицы и 1 новое поле на существующие storage-таблицы.
