# T-7-041. api-schema generation stability check

> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-20
> **Статус задачи в реестре:** review

---

## Что сделано

- Проверена детерминированность генерации `api-schema.yaml` в текущем состоянии репозитория.
- Выполнены два подряд прогона:
  - `python manage.py spectacular --skip-checks --validate --file api-schema.yaml`
  - `python manage.py spectacular --skip-checks --validate --file api-schema.yaml`
- Затем сравнен результат с предыдущей копией файла через `git diff --no-index`.

---

## Результат проверки

- Между двумя подряд генерациями `api-schema.yaml` diff отсутствует.
- Значит, текущая генерация schema в репозитории стабильна; historical broad diff из отчётов `T-7-005/010` не воспроизводится как nondeterministic behavior.

Ограничение:

- Старые `drf-spectacular` warnings/errors по `health`, `dashboard`, path params и duplicate component names всё ещё существуют.
- Эта задача закрывает только аспект **stability / unrelated diff**, а не полную schema-cleanup.

---

## Проверки

- `python manage.py spectacular --skip-checks --validate --file api-schema.yaml`
- повторный `python manage.py spectacular --skip-checks --validate --file api-schema.yaml`
- `git diff --no-index api-schema.before-t7041.yaml api-schema.yaml`
