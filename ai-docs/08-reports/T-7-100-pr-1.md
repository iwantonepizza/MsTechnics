# T-7-100 PR-1. Cleanup dead styles

> **PR:** не открыт
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-20
> **Статус задачи в 03-tasks/:** in-progress

---

## Что сделано

- Обновлён [frontend/index.html](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/index.html): убран хардкод `<html class="dark">`, удалён `Inter`, добавлены preload для `TT Travels`, `theme-color` для light/dark и стартовый класс `no-theme-transition`.
- Полностью заменён [globals.css](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/app/styles/globals.css) на пакет из design round 4, адаптированный к реальному пути проекта `frontend/src/app/styles/globals.css`: CSS-vars вместо мёртвых `bg-surface-*`, плавный theme-transition, `.input`, scrollbar на текущих токенах, legacy `.panel-mono`.
- В конец [tokens.css](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/app/styles/tokens.css) добавлены append-only расширения из handoff: глобальный `:focus-visible`, skeleton height tokens, `bell-pulse`, `mark`, `prefers-reduced-motion`, `forced-colors`, уточнённый touch-density media query.
- Обновлён блок `Toaster` в [App.tsx](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/app/App.tsx): снят `richColors`, toast приведён к brand-token стилям через CSS variables и semantic class names.
- В [theme.tsx](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/shared/lib/theme.tsx) добавлен короткий startup effect, который снимает `no-theme-transition` через 50ms. Без этого glue-кода новый класс из `index.html` оставался бы навсегда и ломал бы acceptance criterion на плавное переключение темы.
- В [globals.css](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/app/styles/globals.css) добавлены правила окраски иконок Sonner (`success/error/warning/info`) по semantic токенам, потому что handoff-snippet для `Toaster` ссылался на эти классы, но сам CSS в пакет не вошёл.

---

## Отклонения от handoff

- **Что:** дополнительно изменён [theme.tsx](/abs/path/C:/Users/miser/mstechnics/mstechnics/frontend/src/shared/lib/theme.tsx).
- **Почему:** пакет `PR-1` вводит `no-theme-transition`, но не содержит фактического снятия класса на runtime. Без этой правки тема оставалась бы без transition всегда.
- **Нужно ли обновить архитектурный документ:** нет, это локальный runtime glue для уже существующего `ThemeProvider`.

- **Что:** добавлены CSS-правила для `sonner-success/error/warning/info`.
- **Почему:** `App.tsx-toaster.snippet` назначает классы toast'ам, но stylesheet с окраской иконок в пакет не включён. Без этих строк semantic icon coloring не применялся бы.
- **Нужно ли обновить архитектурный документ:** нет.

---

## Тесты

- Что покрыто вручную через кодовый аудит:
  - отсутствие хардкода `class="dark"` в `index.html`
  - корректная связка `no-theme-transition` -> снятие через `ThemeProvider`
  - themed Sonner без `richColors`
  - наличие `.input` utility и глобального `:focus-visible`
- Что не покрыто и почему:
  - browser smoke `light <-> dark`, scrollbar, toast visual и tab-focus в этой сессии не прогонялся интерактивно
  - общий `npm run typecheck` в репозитории по-прежнему падает на pre-existing ошибках вне `PR-1`

Проверки:

- [x] `npm run test -- ThemeToggle`
- [ ] `npm run test`
- [ ] `npm run typecheck`
- [ ] ручной frontend smoke в браузере

`npm run typecheck` падает вне `PR-1`:

- `src/features/applications/CreateApplicationModal.tsx`
- `src/features/applications/TransitionModal.tsx`
- `src/features/panels/PanelActionModals.tsx`
- `src/features/panels/PanelRemovalModal.test.tsx`
- `src/pages/login/LoginPage.tsx`
- `src/pages/zip/ZipPage.test.tsx`

---

## Дальнейшие шаги

- Прогнать `frontend`-проверки после `PR-1`.
- Если `PR-1` зелёный, переходить к `PR-2` (`EmptyState` и вынос эмодзи в lucide).
