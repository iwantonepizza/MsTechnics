# T-7-100 PR-12. Automated acceptance sweep

> **PR:** не открыт
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-20
> **Статус задачи в 03-tasks/:** in-progress

---

## Что сделано

- Удалены два мёртвых фронтовых файла, оставшихся после интеграции Round 4:
  - `frontend/src/app/globals.css`
  - `frontend/src/pages/menu/MenuPage.tsx`
- Из `frontend/src/app/styles/globals.css` убраны последние текстовые упоминания legacy-токенов, из-за которых формальный `git grep` по acceptance checklist ещё находил `bg-surface-*` и `text-text-*`.
- Убран лишний hex fallback из `frontend/src/widgets/navigation/NotificationBell.tsx`.
- Обновлён mock в `frontend/src/pages/menu/MainMenuPage.test.tsx`, чтобы hex-цвета не оставались вне разрешённых файлов (`tokens.css` и `ApplicationDetailSheet*`).
- Обновлены snapshot'ы `PanelRemovalModal` под уже интегрированный новый `Modal`.
- Починен confirm-flow:
  - `frontend/src/shared/ui/ConfirmDialog.tsx` теперь не закрывает диалог, если `onConfirm` падает;
  - `frontend/src/features/panels/PanelDeleteButton.tsx` снова может показывать backend-ошибку в диалоге без unhandled rejection;
  - добавлен тест на rejected `onConfirm` в `frontend/src/shared/ui/ConfirmDialog.test.tsx`.

---

## Почему это отдельным sweep

- После `PR-11` cleanup временного design-package в `T-7-100` оставался формальный хвост: acceptance checklist ещё не был доведён до зелёного состояния.
- Это уже не функциональная интеграция конкретного handoff-патча, а добивка итоговых критериев качества по всей задаче.

---

## Проверки

- [x] `git grep -n "bg-surface-" -- frontend/src`
- [x] `git grep -n "text-text-" -- frontend/src`
- [x] `rg -n -P "[\x{1F300}-\x{1FAFF}]" frontend/src -g "!**/*.test.tsx" -g "!**/*.test.ts" -g "!**/__snapshots__/**"`
- [x] `rg -n "#[0-9a-fA-F]{3,8}" frontend/src`
  Результат ограничен `frontend/src/app/styles/tokens.css` и `frontend/src/entities/application/ApplicationDetailSheet*`.
- [x] `npm test -- src/features/panels/PanelRemovalModal.test.tsx -u`
- [x] `npm test -- src/shared/ui/ConfirmDialog.test.tsx`
- [x] `npm test -- src/features/panels/PanelDeleteButton.test.tsx`
- [x] `npm test`

---

## Что ещё остаётся

- Ручная визуальная приёмка light/dark mode по всем экранам.
- Ручная проверка focus ring по Tab-навигации.
- Старый `npm run typecheck` долг вне этого sweep (`T-7-042` и связанные хвосты форм/панелей).

---

## Результат

- Автоматическая часть acceptance checklist `T-7-100` доведена до зелёного состояния.
- Frontend test suite снова полностью зелёный (`16` файлов, `60` тестов).
- `T-7-100` можно доводить до `review` после ручной визуальной приёмки; с кодовой стороны незакрытым остаётся только неавтоматизируемый manual-check слой.
