# T-7-100 PR-13. Restored prod-copy acceptance close-out

> **PR:** не открыт
> **Автор:** GPT-5 Codex
> **Дата:** 2026-05-20
> **Статус задачи в 03-tasks/:** review

---

## Что сделано

- На restored prod-copy добит backend-блокер, который мешал acceptance sweep для `monitoring` display:
  - `apps/workflow/departures/models.py` + `0005_align_departure_status_legacy_column_state.py` выровняли legacy-column state для `Departure.status_legacy` (`db_column="status"`).
  - `apps/interface/api/v1/displays/views.py` починен для detail-actions `contacts/alarms/photos/upload_photo`:
    - router теперь может передавать `slug=...` без `TypeError`;
    - `alarms` больше не режет queryset до `CursorPagination`, из-за чего раньше падал `Cannot reorder a query once a slice has been taken`.
- Добавлен regression-тест `apps/interface/tests/test_display_actions_api.py`.
- Перезапущен backend dev server на temp PostgreSQL-копии `mstechnics_t7100b`, после чего повторно прогнан browser-check для `monitoring` display.

---

## Проверки

- `pytest apps/interface/tests/test_departures_api.py`
- `pytest apps/interface/tests/test_display_actions_api.py`
- `ruff check apps/interface/api/v1/displays/views.py apps/interface/tests/test_display_actions_api.py`
- DRF smoke на temp DB:
  - `GET /api/v1/displays/belinskogo/alarms/?resolved=false&limit=50` → `200`
- Browser recheck на `http://127.0.0.1:5173/monitoring/perm/belinskogo`:
  - light theme
  - dark theme
  - `issuesCount: 0`

Артефакты:

- `logs/t7100b_acceptance_spa/` — полный SPA sweep по экранам
- `logs/t7100b_monitoring_display_recheck_after_restart/` — повторная точечная проверка monitoring display после backend-fix/restart

---

## Результат

- `T-7-100` больше не упирается в backend `500` на restored prod-copy.
- Screenshot-based acceptance для light/dark и focus states по Phase 7 Round 4 доведён до зелёного состояния.
- Задачу можно переводить в `review`.
