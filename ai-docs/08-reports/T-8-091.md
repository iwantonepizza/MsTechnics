# T-8-091

## Что сделано

- В `Config/nginx.conf` добавлен `location /media/ { alias /app/media/; }`.
- Это закрывает 404 на медиа-файлах экранов и фото в production при `DEBUG=False`.

## Проверка

- `python scripts/check_encoding.py`
- `.\.venv\Scripts\python.exe manage.py check`

## Примечания

- Изменение инфраструктурное, без правок Django-логики.
