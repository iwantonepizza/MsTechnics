# T-8-112. Post-cutover infra hardening

Дата: 2026-06-05
Исполнитель: GPT-5

## Сделано в репозитории

- Добавлен production Nginx snippet `infra/nginx/mstechnics-prod-locations.conf`:
  - `/health/live` и `/health/ready` проксируются в DRF health endpoints;
  - `/metrics` оставлен только для `127.0.0.1`/`::1`, наружу должен быть закрыт.
- Актуализированы systemd units под production path `/root/DisplayControl/MsTechnics` и venv `/root/DisplayControl/venv-c84d3816`.
- В `mstechnics-vnnox-pull.service` добавлен `ConditionPathExists=/root/DisplayControl/MsTechnics/Config/token.pickle`, чтобы таймер не падал без Gmail OAuth token.
- Добавлен fail2ban jail `infra/fail2ban/jail.d/mstechnics-sshd.local`.

## Сделано на production

- Закрыт public `/metrics` в Nginx:
  - до изменения: `200 text/plain`;
  - после изменения: public `403`, local `200`.
- Health aliases проверены:
  - `/health/live` -> `200 application/json`;
  - `/health/ready` -> `200 application/json`.
- Установлен и включён `fail2ban`.
- Добавлен `/etc/fail2ban/jail.d/mstechnics-sshd.local` с `backend = systemd`.
- `fail2ban-client status sshd` после включения:
  - `Currently failed: 5`;
  - `Total failed: 386`;
  - `Currently banned: 4`.
- Перед правкой Nginx создан backup:
  `/var/backups/mstechnics/manual-infra-20260605_000818/nginx-before-metrics.conf`.

## Проверено на production

- `mstechnics-vnnox-unresolved.timer` установлен, enabled и active.
- `mstechnics-vnnox-pull.timer` не установлен.
- `Config/client_secret.json` есть, `Config/token.pickle` отсутствует.
- `Display.vnnox_device_id` заполнен у `0` экранов из `8`.
- `AlarmEvent` пустой, открытых faulty alarms нет.
- `mstechnics-db-backup.timer` active, последние локальные dumps создаются в `/var/backups/mstechnics/scheduled`.
- Off-host параметры `BACKUP_REMOTE_TARGET`, `BACKUP_ENCRYPTION_PASSPHRASE`, `BACKUP_ALLOW_PLAINTEXT_REMOTE` не заданы.
- Prometheus/Grafana/Alertmanager systemd/docker services не найдены.
- SSH auth policy не менялась по просьбе владельца: смену пароля выполняет владелец отдельно.

## Что осталось на сервере после push

Production repo подтянут до `47fef6e`, `git status --short` пустой.

## Блокеры владельца

- Для VNNOX auto-import нужен `Config/token.pickle` и заполненные `Display.vnnox_device_id` в Django admin.
- Для off-host backup нужен отдельный backup target и passphrase для `BACKUP_ENCRYPTION_PASSPHRASE`.
- Смену опубликованного SSH-пароля выполняет владелец.

## Текущий статус

T-8-112 закрыта. Server-side hardening применён, repo-side изменения запушены и подтянуты на production.
