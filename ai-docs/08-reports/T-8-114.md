# T-8-114. Mobile monitoring daily tasks on main page

Дата: 2026-06-06
Исполнитель: GPT-5

## Что сделано

- На главной `/menu` мобильный блок ежедневных задач переведён с control-семантики на monitoring-семантику.
- Блок оставлен в потоке страницы под KPI и скрыт на desktop через `md:hidden`, без `fixed`/`absolute`, чтобы не перекрывать остальные блоки.
- Проверка доступа учитывает legacy `permission` и профильные `roles`; пользователь с ролью `monitoring` увидит блок даже при multi-role профиле.
- Старый текст «Задачи контроля» заменён на «Ежедневные задачи» / «Быстрый доступ для мониторинга».
- Дополнительное backend/frontend логирование не добавлялось: изменение frontend-only, а backend уже пишет структурные JSON-логи через `structlog` в stdout/systemd journal.

## Отклонения от плана

Нет отклонений.

## Архитектурные решения

- Новый API не добавлялся: переиспользован существующий `DailyTasksPanel` и endpoint `/api/v1/daily-tasks/`.
- `roles` обработаны как `unknown`, потому что OpenAPI snapshot сейчас типизирует поле как `string`, а DRF serializer фактически возвращает список ролей.

## Тесты

- Новых файлов с тестами: 0
- Добавлено тестов: 1
- Обновлено тестов: 1
- Coverage нового кода: N/A, изменение покрыто существующим unit-тестом helper-а.
- Проверки:
  - `npm --prefix frontend test -- MainMenuPage.test.tsx DailyTasksPanel.test.tsx` -> 2 files / 10 tests passed.
  - `npm --prefix frontend test` -> 28 files / 113 tests passed.
  - `npm --prefix frontend run build` -> passed.
  - `python scripts/check_encoding.py frontend/src/pages/menu/MainMenuPage.tsx frontend/src/pages/menu/MainMenuPage.test.tsx ai-docs/03-tasks/phase-8-owner-feedback/T-8-114-mobile-monitoring-main-daily-tasks.md` -> passed.
  - `python scripts/check_encoding.py` -> 842 text files checked, passed.

## Нагрузка / производительность

N/A. Новый endpoint не добавлялся. Блок использует существующий query-hook ежедневных задач.

## Миграции

N/A.

## Production deploy

- Deployed 2026-06-06 через SSH на `root@185.251.88.121`.
- Server repo: `df0dbfa` -> `39352c8` через `git pull --ff-only origin main`.
- На сервере остался один unrelated untracked media-файл:
  `media/files/17741197601493673483.jpg`; его не трогали.
- `python manage.py check` -> `System check identified no issues`.
- `npm --prefix frontend run build` -> passed, новый bundle:
  `static/spa/assets/index-C9IkfIHW.js`.
- `python manage.py collectstatic --noinput` -> `50 static files copied`, `191 unmodified`.
- `systemctl restart gunicorn` -> passed; `gunicorn` active with gevent workers.
- Public smoke:
  - `https://mstechnics.ru/` -> `assets/index-C9IkfIHW.js`;
  - `https://mstechnics.ru/health/live` -> `200 application/json`;
  - `https://mstechnics.ru/health/ready` -> `200 application/json`;
  - `https://mstechnics.ru/menu` -> `200 text/html`;
  - production JS contains `monitoring-mobile-tasks`, `Ежедневные задачи`,
    `Быстрый доступ для мониторинга`.
- Logs:
  - `journalctl -u gunicorn --since '15 minutes ago'` after restart contains only normal
    stop/start/worker boot records;
  - grep for `error|exception|traceback|critical|failed|disallowed` in fresh gunicorn logs -> empty;
  - `journalctl -u nginx --since '15 minutes ago'` -> no entries;
  - `/var/log/nginx/error.log` tail contains only old scanner/static-miss entries before deploy,
    no fresh deploy-related application errors.

## Статус

Done. Код запушен и развёрнут на production.
