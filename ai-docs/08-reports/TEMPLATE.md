# T-X-XXX. <Название задачи>

> **PR:** <ссылка на PR, заполняется при открытии>
> **Автор:** <кто делал>
> **Дата:** YYYY-MM-DD
> **Статус задачи в 03-tasks/:** review → done

---

## Что сделано

Краткий список того, что реально получилось. В виде диффа от плана задачи.

- ✅ Пункт 1 из плана — сделано как было описано
- ✅ Пункт 2 — сделано, но с отклонением (см. ниже)
- ❌ Пункт 3 — не сделано, причина: ...

---

## Отклонения от плана

Если что-то делал иначе, чем в задаче — опиши здесь:

- **Что:** ...
- **Почему:** ...
- **Нужно ли обновить архитектурный документ:** (да/нет, какой)

Если не делал ничего иначе — напиши «Нет отклонений».

---

## Архитектурные решения

Если пришлось принять небольшое локальное решение (выбор имени функции, структуры папок, паттерна):

- Решение 1: почему так, а не иначе
- ...

Большие решения (затрагивающие несколько модулей) — **не принимай сам**, спроси архитектора. Если всё же принял — оформляй ADR в `ai-docs/adr/`.

---

## Тесты

- Новых файлов с тестами: N
- Добавлено тестов: M
- Coverage нового кода: X%
- Что покрыто:
  - Happy path
  - Edge case 1 (например: пустой список)
  - Edge case 2 (например: несуществующий ID)
  - Error path (например: 403 для чужого города)
- Что НЕ покрыто и почему:
  - ...

---

## Нагрузка / производительность

Если задача затрагивает перформанс:

- До: N запросов к БД / M мс отклика
- После: N' запросов / M' мс
- Как измерял: (Debug Toolbar, django-silk, `timeit`, etc.)

Если не затрагивает — пиши «N/A».

---

## Миграции

Если есть миграции:

- Количество миграций: N
- Тип: structure / data / mixed
- Протестировано на копии прод-БД: (да/нет, если нет — почему)
- Оценка времени на проде: X секунд / минут
- План отката: ...

Если нет миграций — «N/A».

---

## Время

- Оценка в задаче: N часов
- Фактически: M часов
- Если M > N*1.5: что недооценили, можно ли было предвидеть

---

## Проверки перед PR

- [ ] `pre-commit run --all-files` — зелёное
- [ ] `pytest` — зелёное
- [ ] `mypy` — зелёное
- [ ] Ручное тестирование: ...
- [ ] Нет debug-кода
- [ ] Нет секретов
- [ ] PR-description заполнен по шаблону

---

## Скриншоты / демо

Если задача меняет UI или внешний вид админки — приложи 1-2 скриншота (в виде ссылок на вложения PR, не в репо!).

---

## Дальнейшие шаги

Что из этой задачи вытекает, но было **вне скоупа**:

- Новая задача 1: ... → завёл в `03-tasks/T-X-YYY.md` / предлагаю завести
- Техдолг: ... → отмечено в `01-current-state/audit-report.md` или новой записью

---

## Вопросы архитектору / ретро

Что показалось неоднозначным, что хорошо бы сделать лучше в следующий раз:

- ...
