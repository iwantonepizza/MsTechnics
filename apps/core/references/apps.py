from django.apps import AppConfig


class ReferencesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.core.references"
    label = "core_references"
    verbose_name = "Справочники"
