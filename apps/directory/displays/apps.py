from django.apps import AppConfig


class DisplaysConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.directory.displays"
    label = "directory_displays"
    verbose_name = "Экраны"
