from django.apps import AppConfig


class PanelsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.directory.panels"
    label = "directory_panels"
    verbose_name = "Панели"
